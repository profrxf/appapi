[project]
name = "git"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "groq>=1.2.0",
    "ruff>=0.15.13",
]

[tool.uv.workspace]
members = [
    "pos_fastapi",
]
